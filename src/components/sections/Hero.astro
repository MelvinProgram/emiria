---
import { Image } from "astro:assets";
import heroImage from "/public/images/hero-product.jpg";
---
<section class="relative overflow-hidden">
  <div class="mx-auto max-w-7xl px-4 py-16 md:py-24">
    <div class="grid gap-12 md:grid-cols-2 md:items-center">

      <!-- Texto -->
      <div class="text-center md:text-left">
        <p class="mb-4 inline-block rounded-full bg-[#9B9B7E]/10 px-4 py-1 text-xs tracking-wide uppercase text-[#9B9B7E]">
          Cosmética capilar premium
        </p>

        <h1 class="font-serif text-4xl leading-tight md:text-5xl lg:text-6xl">
          Cabello sano, elegante y
          <span class="italic">natural</span>
        </h1>

        <p class="mt-6 max-w-md mx-auto md:mx-0 text-base md:text-lg opacity-80">
          Fórmulas limpias diseñadas para fortalecer, reparar y devolver
          el brillo natural a tu cabello desde la raíz.
        </p>

        <div class="mt-8 flex flex-col gap-4 sm:flex-row sm:justify-center md:justify-start">
          <a
            href="#productos"
            class="inline-flex items-center justify-center rounded-full bg-[#9B9B7E] px-8 py-4 text-sm font-medium text-white transition hover:opacity-90"
          >
            Comprar ahora
          </a>

          <a
            href="#ingredientes"
            class="inline-flex items-center justify-center rounded-full border border-black/10 px-8 py-4 text-sm transition hover:bg-black/5"
          >
            Ver ingredientes
          </a>
        </div>
      </div>

      <!-- Imagen LCP optimizada -->
      <div class="relative">
        <div class="aspect-4/5 overflow-hidden rounded-3xl bg-[#eae7e1]">
          <Image
            src={heroImage}
            alt="Producto cosmético capilar para cabello rizado"
            widths={[480, 768, 1024, 1440]}
            sizes="(max-width: 768px) 100vw, 50vw"
            format="webp"
            loading="eager"
            priority
            class="h-full w-full object-cover"
          />
        </div>
      </div>

    </div>
  </div>
</section>